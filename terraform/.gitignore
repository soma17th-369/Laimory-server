# Terraform state — 비밀 포함 가능, 절대 커밋 금지
*.tfstate
*.tfstate.*
*.tfstate.backup

# 로컬 provider 캐시
.terraform/
crash.log
crash.*.log

# 비밀 변수 파일 (예시 파일만 커밋)
*.auto.tfvars
!*.auto.tfvars.example

# override 파일
override.tf
override.tf.json
*_override.tf
*_override.tf.json
