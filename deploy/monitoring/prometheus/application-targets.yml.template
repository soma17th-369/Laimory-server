# Rendered by render-prometheus-targets.py; do not edit generated output.
- targets:
    - "${dev_was_private_ip}:9090"
  labels:
    application: "laimory"
    environment: "dev"
    host: "dev-was"
