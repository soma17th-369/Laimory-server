interface:
  display_name: "Merge PR"
  short_description: "Publish a PR digest comment, verify gates, squash merge, and close linked issues."
  default_prompt: "Use $merge-pr to publish the current pull request digest comment, verify merge gates, squash merge it, and close the issues it links."
policy:
  allow_implicit_invocation: false
