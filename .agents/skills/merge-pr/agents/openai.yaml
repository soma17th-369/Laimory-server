interface:
  display_name: "Merge PR"
  short_description: "Publish a PR digest comment, verify gates, and squash merge."
  default_prompt: "Use $merge-pr to publish the current pull request digest comment, verify merge gates, and squash merge it."
policy:
  allow_implicit_invocation: false
