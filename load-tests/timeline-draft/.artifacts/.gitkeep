이 파일은 artifact 격리 sentinel이다.

`load-tests/timeline-draft/.gitignore`가 이 디렉터리의 나머지 전부를 무시하고 이 sentinel만 추적한다.
`scripts/verify-artifact-hygiene.sh`가 sentinel 추적 여부와 ignore 규칙 동작을 함께 검증한다.

이 디렉터리에는 access token, user/task manifest, k6 raw 결과와 실행 로그만 둔다. 절대 커밋하지 않는다.
